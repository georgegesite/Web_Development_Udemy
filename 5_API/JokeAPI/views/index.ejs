<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joke Api</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        .joke-container {
            background-color: #ffffff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            margin: 20px;
        }

        .joke-text {
            font-size: 20px;
            margin: 0;
            padding: 10px 0;
        }

        .category-form {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 20px;
        }

        .category-input {
            padding: 8px;
            border: none;
            border-radius: 4px;
            margin-right: 10px;
            font-size: 14px;
        }

        .submit-button {
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            cursor: pointer;
            font-size: 14px;
        }

        .submit-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="joke-container">
        <% if (locals.theJoke) { %>
            <p class="joke-text"><%= theJoke %></p>
        <% }  else  {%>
            <p class="joke-text">Enter category: "Programming", "Misc", "Dark", "Pun", "Spooky", "Christmas" </p>
       <% } %>
        <form class="category-form" action="/submit" method="POST">
            <input class="category-input" type="text" name="answer" placeholder="Enter category">
            <button class="submit-button" type="submit">OK</button>
        </form>
    </div>
</body>
</html>
